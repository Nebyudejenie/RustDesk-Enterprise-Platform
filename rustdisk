Phase 1 — MVP Docker + Unattended Access Core

You are a senior DevOps and infrastructure architect. Build a complete, production-grade, 100% working Phase 1 foundation for a self-hosted RustDesk enterprise remote access platform running on Ubuntu 24.04 LTS inside Proxmox VE.

ENVIRONMENT:
- Proxmox VE hypervisor
- Ubuntu 24.04 LTS VM — SSH: cosmic@192.168.1.40
- Docker + Docker Compose (Phase 1 runtime)
- Future: Kubernetes on AKS, Azure DevOps CI/CD

CORE BUSINESS REQUIREMENT:
POS support engineers must type only the POS device ID and instantly connect — no approval popup, no confirmation dialog, no user interaction on the POS side. This is non-negotiable.

PHASE 1 DELIVERABLES — produce every file completely, no placeholders:

1. RUSTDESK SERVER SETUP
   - Full docker-compose.yml deploying hbbs + hbbr with correct port mappings
   - Persistent volume mounts for keys and data
   - Environment variables for custom relay host
   - Health checks on both services
   - Automatic restart policies
   - Log rotation config

2. UNATTENDED ACCESS CONFIGURATION
   - Exact RustDesk client config file (RustDesk2.toml) that:
     * Pre-sets permanent password (fixed, not one-time)
     * Disables all approval prompts
     * Disables all permission dialogs
     * Sets custom relay/signal server addresses
     * Enables auto-accept incoming connections
     * Disables lock screen on disconnect
     * Sets service to start before user login (system service)
   - Produce the EXACT config values — no "fill in your value" comments

3. WINDOWS SILENT INSTALLER (for POS devices running Windows 10/11)
   - PowerShell script that:
     * Downloads RustDesk MSI silently
     * Injects the pre-configured RustDesk2.toml
     * Installs as Windows service (runas SYSTEM)
     * Sets service to auto-start
     * Disables Windows firewall rules blocking RustDesk ports
     * Registers device with a custom ID prefix (e.g. POS-ADDIS-001)
     * Suppresses all UI during install
     * Logs install result to C:\RustDesk\install.log
     * Self-deletes the installer after completion
   - One-liner deployment command using Invoke-WebRequest

4. LINUX SILENT INSTALLER (for Linux POS devices or Ubuntu VMs)
   - Bash script performing identical silent setup
   - Systemd service unit file
   - Auto-reconnect on network failure
   - One-liner curl | bash deployment command

5. FOLDER STRUCTURE
   - Full project directory tree for /opt/rustdesk-platform/
   - All files listed with their exact paths

6. FIREWALL & NETWORK
   - UFW rules for the Ubuntu server
   - Required open ports with explanation
   - Internal LAN vs external access separation

7. VERIFICATION CHECKLIST
   - Exact commands to verify every component is working
   - How to confirm unattended access works without touching the POS device

Output format: Every file in full. Every command ready to copy-paste. Zero placeholders. Zero "TODO" comments. Production-ready for an Ethiopian enterprise environment with mixed internet stability.

Phase 2 — Kubernetes Production Deployment

You are a senior Kubernetes and cloud-native architect. Build a complete, production-ready Kubernetes deployment for a self-hosted RustDesk enterprise platform.

CONTEXT:
- RustDesk hbbs (signal) and hbbr (relay) servers must run in Kubernetes
- Starting on local Kubernetes (can be k3s or kubeadm on Ubuntu 24.04)
- Future migration path to AKS (Azure Kubernetes Service)
- SSH access to base VM: cosmic@192.168.1.40
- Docker images: rustdesk/rustdesk-server-s6 (or individual hbbs/hbbr images)

HARD CONSTRAINTS:
- hbbs and hbbr must have stable, predictable IPs or DNS — POS clients embed the server address
- Keys generated by hbbs must persist across pod restarts — use PersistentVolumeClaims
- UDP ports must work — many Kubernetes setups break UDP, solve this explicitly
- No NodePort 3xxxx — use hostNetwork: true or LoadBalancer for clean port exposure

DELIVERABLES — every file complete, no placeholders:

1. KUBERNETES MANIFESTS (production-grade)
   - Namespace: rustdesk-system
   - hbbs Deployment with:
     * readinessProbe + livenessProbe
     * resource requests and limits
     * anti-affinity rules for HA
     * PVC for /root volume (key persistence)
   - hbbr Deployment (same standards)
   - Services for both:
     * hbbs: TCP 21115, 21116, 21118 + UDP 21116
     * hbbr: TCP 21117, 21119
   - PersistentVolumeClaims for both
   - ConfigMap for environment variables
   - Secret manifest for relay key (base64 encoded)
   - HorizontalPodAutoscaler for hbbr (relay scales, signal does not)
   - NetworkPolicy restricting ingress to only required ports
   - PodDisruptionBudget for HA

2. INGRESS / LOAD BALANCER
   - MetalLB config (for bare metal / local cluster)
   - LoadBalancer service exposing stable external IP
   - Explain why Ingress controllers (nginx/traefik) do NOT work for RustDesk TCP/UDP and how to work around it

3. STORAGE
   - StorageClass definition (local-path or Longhorn)
   - PVC for hbbs key storage — explain exactly why this is critical
   - Backup CronJob for the key volume using kubectl cp or Velero

4. HELM CHART STRUCTURE
   - Full Helm chart: Chart.yaml, values.yaml, all templates
   - values.yaml covering: image tags, resource limits, service type, storage size, relay host, custom password
   - Parameterized for dev/staging/production

5. K3S INSTALLATION
   - Exact k3s install command for Ubuntu 24.04
   - Post-install kubeconfig setup
   - Verify cluster is ready commands

6. MIGRATION PATH TO AKS
   - What changes between local k3s and AKS
   - AKS-specific service annotations
   - Azure Disk vs Azure File for PVC

7. GITOPS SETUP
   - FluxCD or ArgoCD manifest to sync from a GitHub repo
   - Folder structure for the GitOps repo

Output: Every YAML in full. All kubectl apply commands. Validation commands after each step. Explain every non-obvious decision.

Phase 3 — Security Hardening & Zero Trust

You are a senior security architect and DevSecOps engineer. Harden a self-hosted RustDesk enterprise platform to production security standards suitable for an enterprise environment operating POS devices across multiple Ethiopian retail locations.

SYSTEM CONTEXT:
- Ubuntu 24.04 LTS VM on Proxmox (cosmic@192.168.1.40)
- RustDesk hbbs + hbbr in Kubernetes
- Cloudflare Tunnel for external access
- Windows POS devices connecting as unattended clients
- Support engineers connecting from office or remote

THREAT MODEL:
- Unauthorized access to POS devices (financial data risk)
- Compromised support engineer credentials
- Man-in-the-middle on public networks
- Rogue devices registering with fake IDs
- Brute force against relay server
- Insider threat from support staff

DELIVERABLES — every config file complete:

1. RUSTDESK SECURITY CONFIG
   - Permanent password policy (minimum entropy, no dictionary words)
   - How to rotate passwords across all POS devices via script
   - Key pair management — where hbbs keys are stored, how to back them up
   - Exactly how to verify TLS is active between client and server
   - Config to prevent ID enumeration (sequential POS IDs are a risk — explain mitigation)

2. OS HARDENING (Ubuntu 24.04)
   - CIS Benchmark Level 1 hardening script (bash, complete)
   - SSH hardening: disable root, key-only auth, change port, fail2ban config
   - UFW rules: exact rules for RustDesk ports + management
   - Auditd rules for monitoring RustDesk process activity
   - Unattended-upgrades config for auto security patches
   - /etc/sysctl.conf hardening (network stack)

3. KUBERNETES SECURITY
   - PodSecurityAdmission policies (restricted profile)
   - RBAC roles: admin, support-engineer, readonly-auditor
   - Kubernetes NetworkPolicy (deny-all default + allow only required)
   - Secrets management: seal secrets with Sealed Secrets or use External Secrets Operator with Azure Key Vault
   - Pod security context: runAsNonRoot, readOnlyRootFilesystem, drop ALL capabilities
   - Admission webhook: OPA Gatekeeper or Kyverno policy to enforce resource limits

4. ACCESS CONTROL
   - Support engineer RBAC model: who can connect to what POS group
   - Device grouping: Branch A, Branch B, etc. — how to enforce group-level access
   - Audit log: every connection attempt logged with engineer ID, device ID, timestamp, duration
   - Connection approval bypass audit trail — prove unattended access is authorized

5. NETWORK SECURITY
   - mTLS between support client and relay server (if supported)
   - VPN alternative analysis: WireGuard vs Cloudflare Tunnel vs Tailscale for this use case
   - Rate limiting on relay port 21117
   - IP allowlist for support engineer source IPs (if static)
   - DDoS mitigation for publicly exposed relay

6. WINDOWS POS HARDENING
   - Windows Defender config to whitelist RustDesk without disabling AV
   - AppLocker or WDAC policy to allow only signed RustDesk binary
   - Restrict RustDesk config from being modified by local POS user
   - Service account with minimal privileges for RustDesk service

7. INCIDENT RESPONSE
   - How to immediately revoke access to a specific POS device
   - How to rotate keys across entire fleet after a breach
   - Emergency lockdown procedure
   - Forensic logging locations

8. COMPLIANCE NOTES
   - PCI-DSS relevant controls for remote access to POS
   - Data minimization: what RustDesk logs and how to control it

All configs complete. No "configure as appropriate" comments. Production values only.

Phase 4 — PostgreSQL + REST API Backend

You are a senior backend engineer and database architect. Build a complete API backend and PostgreSQL database layer for a self-hosted RustDesk enterprise remote access management platform.

SYSTEM CONTEXT:
- RustDesk server handles raw remote connections
- This API is the management plane: device registry, engineer auth, audit logs, device health
- Runtime: Docker / Kubernetes on Ubuntu 24.04
- Database: PostgreSQL 16
- API: FastAPI (Python) — chosen for speed of development and async support
- Auth: JWT access tokens + refresh tokens + API keys for device agents
- Future: AI agent integration will call this API autonomously

DELIVERABLES — every file complete, production-ready:

1. POSTGRESQL SCHEMA (complete, runnable SQL)
   - Table: devices
     * device_id (custom prefix like POS-ADDIS-001), rustdesk_id, location_id, branch_id
     * device_type, os_version, rustdesk_version
     * permanent_password_hash (bcrypt)
     * registered_at, last_seen_at, last_ip, status (online/offline/maintenance)
     * tags (JSONB), metadata (JSONB)
   - Table: locations / branches (multi-location hierarchy)
   - Table: engineers
     * engineer_id, full_name, email, password_hash, role (admin/support/auditor)
     * allowed_branch_ids (array), mfa_secret, last_login
   - Table: connections (audit log)
     * connection_id, engineer_id, device_id, started_at, ended_at, duration_seconds
     * source_ip, session_key, disconnect_reason, was_unattended
   - Table: device_heartbeats
     * device_id, timestamp, cpu_percent, ram_percent, disk_percent, network_status
   - Table: alerts
     * alert_id, device_id, alert_type, severity, message, resolved_at
   - Indexes: on device_id, last_seen_at, engineer_id, branch_id
   - Migrations: using Alembic (include env.py and first migration file)
   - Seed data: 3 test branches, 5 test devices, 2 engineers

2. FASTAPI APPLICATION (full working code)
   Structure:
   app/
     main.py
     config.py (pydantic settings from env vars)
     database.py (async SQLAlchemy + asyncpg)
     models/ (SQLAlchemy ORM models matching schema above)
     schemas/ (Pydantic v2 request/response models)
     routers/
       auth.py       — login, refresh, logout, API key issue
       devices.py    — register, list, get, update status, deregister
       connections.py — log connection start/end, get history
       heartbeat.py  — device heartbeat ingestion endpoint
       engineers.py  — CRUD for engineers, role assignment
       alerts.py     — create, list, resolve alerts
       dashboard.py  — summary stats endpoint
     services/
       auth_service.py    — JWT logic, bcrypt, API key generation
       device_service.py  — business logic layer
       alert_service.py   — alert evaluation rules
     middleware/
       logging.py    — structured JSON request logging
       rate_limit.py — slowapi rate limiting per endpoint
     utils/
       pagination.py
       validators.py

   All routers: complete working code, not pseudocode.
   Auth: every endpoint protected. Device endpoints use API key auth. Engineer endpoints use JWT.

3. DEVICE REGISTRATION FLOW
   - Sequence: POS boots → installer calls /api/v1/devices/register with device_id + api_key → server validates and stores → returns connection credentials
   - Complete POST /api/v1/devices/register endpoint code
   - Auto-assign branch from device_id prefix

4. HEARTBEAT SYSTEM
   - Device sends heartbeat every 60 seconds: POST /api/v1/heartbeat
   - Background task: mark devices offline if no heartbeat for 3 minutes
   - APScheduler background job running inside FastAPI

5. DOCKER COMPOSE ADDITION
   - Add PostgreSQL 16 + FastAPI + pgAdmin to existing docker-compose.yml
   - Environment variables via .env file
   - Depends_on with health checks
   - Named volumes for postgres data

6. KUBERNETES ADDITIONS
   - PostgreSQL StatefulSet with PVC
   - FastAPI Deployment + Service + HPA
   - Secret for DB credentials
   - ConfigMap for API config
   - Liveness and readiness probes

7. API DOCUMENTATION
   - OpenAPI schema summary of all endpoints
   - Example curl commands for every endpoint
   - Postman collection JSON (complete)

Output: Every Python file complete. Every SQL statement runnable. Every YAML deployable. No TODOs.

Phase 5 — Cloudflare Tunnel + Reverse Proxy + Monitoring

You are a senior network engineer and SRE. Build a complete Cloudflare Tunnel integration, reverse proxy configuration, observability stack, and CI/CD pipeline for a self-hosted RustDesk enterprise platform.

SYSTEM CONTEXT:
- Ubuntu 24.04 LTS on Proxmox — cosmic@192.168.1.40
- RustDesk hbbs + hbbr in Kubernetes
- FastAPI management API in Kubernetes
- Future web dashboard in Kubernetes
- Goal: zero exposed public ports on the Proxmox host — all traffic via Cloudflare Tunnel
- Domain: your company domain on Cloudflare DNS

CRITICAL CONSTRAINT:
RustDesk relay (hbbr) uses raw TCP/UDP on port 21117 — Cloudflare HTTP tunnels do NOT proxy raw TCP in the free tier. Solve this problem completely with one of:
  Option A: Cloudflare Spectrum (paid) — explain cost and setup
  Option B: Cloudflared TCP tunnel with --ingress rules — explain limitations
  Option C: Hybrid: Cloudflare for API/dashboard HTTP, WireGuard for relay TCP
  Recommend the best option for Ethiopian SME enterprise context and implement it fully.

DELIVERABLES:

1. CLOUDFLARE TUNNEL SETUP
   - cloudflared install on Ubuntu 24.04 as systemd service
   - Tunnel creation commands (cloudflared tunnel create)
   - config.yml for cloudflared with ingress rules:
     * api.yourdomain.com → FastAPI service (HTTPS)
     * dashboard.yourdomain.com → Web dashboard (HTTPS)
     * relay.yourdomain.com → RustDesk relay (TCP/raw — with chosen solution)
   - Cloudflare DNS CNAME records to create (exact commands)
   - Zero Trust Access policy: protect API with Cloudflare Access (email OTP or GitHub SSO)
   - Kubernetes deployment of cloudflared as a Deployment (not just systemd)

2. NGINX REVERSE PROXY (internal layer)
   - nginx.conf for internal reverse proxy in front of FastAPI
   - SSL termination config (even if Cloudflare handles outer TLS)
   - Rate limiting directives
   - Security headers (HSTS, CSP, X-Frame-Options, etc.)
   - Upstream health check config
   - Access log format (JSON structured)

3. MONITORING STACK (Kubernetes-deployed)
   - Prometheus Deployment + ServiceMonitor
   - Grafana Deployment with pre-built dashboards for:
     * RustDesk connection counts
     * Device online/offline ratio
     * API request rate and latency (p50, p95, p99)
     * Kubernetes pod health
     * Node resource usage
   - AlertManager config with alert rules:
     * Device offline > 10 minutes → warning
     * Device offline > 30 minutes → critical
     * API error rate > 5% → alert
     * Pod crash loop → alert
   - Prometheus scrape configs for FastAPI (/metrics endpoint via prometheus-fastapi-instrumentator)

4. LOGGING STACK
   - Loki + Promtail deployment (lightweight alternative to ELK)
   - Promtail config to scrape:
     * Kubernetes pod logs
     * RustDesk hbbs/hbbr logs
     * FastAPI JSON logs
     * Nginx access logs
   - Grafana Loki datasource config
   - Log retention: 30 days

5. CI/CD PIPELINE (GitHub Actions)
   - Repository structure for the whole project
   - .github/workflows/deploy.yml:
     * Trigger: push to main
     * Jobs: lint → test → build Docker image → push to GitHub Container Registry → kubectl apply
     * Secrets: KUBECONFIG, GHCR_TOKEN
     * Environment-based deployment (staging vs production)
   - Dockerfile for FastAPI (multi-stage, minimal image)
   - .dockerignore

6. BACKUP STRATEGY
   - CronJob: daily PostgreSQL dump to Azure Blob Storage or local PVC
   - CronJob: backup RustDesk hbbs key volume
   - Retention: 30 daily, 12 weekly, 6 monthly
   - Restore procedure: exact commands

7. BANDWIDTH OPTIMIZATION (critical for Ethiopia)
   - RustDesk relay QoS settings
   - Network interface tuning (sysctl) for high connection counts
   - Cloudflare Argo Smart Routing recommendation
   - Fallback: what happens when Cloudflare tunnel drops — local LAN fallback

All configs complete. All commands copy-paste ready. All Kubernetes YAMLs deployable.

Phase 6 — AI Automation Layer

You are a senior AI engineer and autonomous systems architect. Design and build an AI automation layer on top of an existing enterprise RustDesk remote access platform for POS device management.

EXISTING SYSTEM:
- RustDesk self-hosted (hbbs + hbbr) in Kubernetes
- FastAPI management API with PostgreSQL
- Device heartbeat every 60 seconds (CPU, RAM, disk, network)
- Full audit log of all remote connections
- Devices: Windows POS systems across Ethiopian retail branches

AI INTEGRATION GOAL:
Build an autonomous AI agent layer that monitors POS devices, diagnoses issues, takes corrective actions, and only escalates to human engineers when necessary.

DELIVERABLES:

1. AI DIAGNOSTIC AGENT (Python, production code)
   Architecture: LangChain or direct Anthropic API (claude-3-5-sonnet)
   Agent capabilities:
   - Reads device heartbeat data from PostgreSQL
   - Identifies anomaly patterns (CPU spike, RAM leak, disk full, network drop)
   - Calls RustDesk API to initiate unattended connection to affected device
   - Executes predefined remediation scripts via remote session
   - Writes diagnosis report to database
   - Pages engineer only when autonomous fix fails

   Implement:
   - agent/main.py — main agent loop (runs every 5 minutes via APScheduler)
   - agent/tools.py — LangChain tools:
     * get_device_status(device_id) → queries heartbeat table
     * list_offline_devices() → returns devices not seen in 5 min
     * get_device_history(device_id, hours=24) → time series data
     * trigger_remediation(device_id, script_name) → calls remediation API
     * create_alert(device_id, severity, message) → writes alert
     * notify_engineer(engineer_id, message) → sends notification
   - agent/prompts.py — system prompt for the diagnostic agent (complete, production-quality)
   - agent/remediation_scripts/ — Windows batch/PowerShell scripts the agent can trigger:
     * restart_pos_service.bat
     * clear_temp_files.ps1
     * check_network_adapters.ps1
     * restart_rustdesk_service.ps1

2. ANOMALY DETECTION (statistical, no LLM needed for this layer)
   - Python service using Z-score or IQR on 1-hour rolling window of heartbeat data
   - Alert if CPU > 85% for 5+ consecutive readings
   - Alert if RAM > 90% sustained
   - Alert if disk > 95%
   - Alert if heartbeat gap > 3 minutes (device offline)
   - Write to alerts table and trigger AI agent

3. PREDICTIVE MAINTENANCE
   - Simple time-series analysis: if disk fill rate > X GB/day, predict days until full
   - If device has had 3+ crashes in 7 days, flag for physical inspection
   - Weekly summary report generated by AI agent and emailed to admin

4. NATURAL LANGUAGE DASHBOARD QUERY
   - FastAPI endpoint: POST /api/v1/ai/query
   - Engineer sends: {"question": "Which branches had the most offline devices this week?"}
   - Backend: AI agent converts to SQL, runs it, returns human-readable answer
   - Implement NL-to-SQL with Anthropic API + schema context injection
   - Safety: SQL must be SELECT only, no mutations allowed

5. AUTO-PROVISIONING AGENT
   - When a new device registers via API, AI agent:
     * Validates device_id format
     * Assigns correct branch from prefix
     * Generates strong permanent password
     * Creates device record
     * Triggers silent config push to device (via webhook or queue)
     * Sends confirmation to admin

6. FASTAPI AI ENDPOINTS (add to existing API)
   - POST /api/v1/ai/query — natural language query
   - POST /api/v1/ai/diagnose/{device_id} — trigger on-demand AI diagnosis
   - GET /api/v1/ai/report/weekly — AI-generated fleet summary
   - GET /api/v1/ai/predictions — list predictive maintenance alerts

7. NOTIFICATION SYSTEM
   - Telegram Bot integration (best for Ethiopian enterprise — works on mobile data)
   - Send alerts to engineer Telegram when AI cannot auto-resolve
   - Daily digest message to admin: online devices, incidents, AI actions taken
   - Telegram bot commands: /status, /offline, /device POS-ADDIS-001

8. KUBERNETES DEPLOYMENT FOR AI LAYER
   - Deployment for the AI agent service
   - CronJob for weekly report generation
   - Secret for Anthropic API key
   - ConfigMap for agent parameters
   - Resource limits (AI agent can be memory intensive)

Output: All Python files complete and runnable. All API endpoints implemented. All Kubernetes YAMLs production-ready. Telegram bot fully functional. This must be deployable, not a design document.